# Config quick note

Edit only `project_config.json`.

All raw input paths stay local on your machine.
The repository is designed so the public GitHub version tracks:
- code
- docs
- figures/tables
- `data/final/final_suitability.tif`
